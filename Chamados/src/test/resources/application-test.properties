# Banco proprio do teste, separado do banco da aplicacao, e sem o data.sql de
# demonstracao: cada teste monta os dados de que precisa e nada vem pronto.
spring.datasource.url=jdbc:h2:mem:chamados-test;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=never
